#!/usr/bin/env julia
using DataIndexer

length(ARGS) != 1 && error("Please specify the path you want to index!")

ROOTDIR = ARGS[1]
# DataIndexer.DEBUG[] = true

dataindex(ROOTDIR)

println("zz, have a nice day!")
